# include <stdio.h>

int main() {
             int total_distance = 0;
             int fuel_consumed = 0;

             total_distance = 10 + 10 + 5 + 15;
             fuel_consumed = 2*total_distance;
             printf("Total distance: %d", total_distance);
             printf("\nTotal fuel consumed: %d", fuel_consumed); 
}
